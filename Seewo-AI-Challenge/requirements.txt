flask==3.1.0
# Dev / test / deploy / lint (Phase 0 + Phase 1, merged from requirements-dev.txt)
pytest==8.3.3
pytest-flask==1.3.0
gunicorn==23.0.0
ruff==0.6.9
# MIG-03: bcrypt for password hashing (replaces SHA-256 + hardcoded salt in demo/security.py)
bcrypt>=4.0,<6.0
# flask-wtf + flask-limiter are recommended for production (Phase 1+);
# uncomment when ready to swap from in-memory CSRF/rate-limit to extensions:
# flask-wtf==1.2.2
# flask-limiter==3.8.0

# V1.0 Sprint 1: DB + LLM + infra dependencies
sqlalchemy>=2.0,<3.0
alembic>=1.13,<2.0
psycopg2-binary>=2.9,<3.0
redis>=5.0,<6.0
# V2.0 Sprint 7: MFA TOTP (7.1)
pyotp>=2.9,<3.0
qrcode>=7.4,<9.0
